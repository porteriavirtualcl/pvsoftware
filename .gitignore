node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.production
!.env.capacitor

# Firebase service account — contains private keys, never commit
serviceAccountKey.json

# WhatsApp session data — runtime only, must never be overwritten by git pull
wa_sessions/

# PM2 logs
logs/

# Claude Code local config
.claude/

# Android Studio IDE state
android/.idea/
