// Single source of truth for the app version shown in the profile modal.
// Keep `version` aligned with package.json on every release.
export const APP_VERSION = '1.1.1';
export const APP_RELEASE_DATE = '13-05-2026';
