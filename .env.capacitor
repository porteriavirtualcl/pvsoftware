# Build para Capacitor (APK Android).
# Vite carga este archivo cuando se corre `vite build --mode capacitor`.
#
# La APK no tiene servidor Express adentro: todas las llamadas /api/* y /dahua/*
# se redirigen a Hostinger por URL absoluta.
VITE_API_BASE_URL=https://app.porteriavirtual.cl
